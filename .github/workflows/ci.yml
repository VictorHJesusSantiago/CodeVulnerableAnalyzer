name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install (with dev extras)
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      # Lint bloqueante — aplica a política de "zero warnings" do audit.
      - name: Ruff (lint)
        run: ruff check analyzer main.py

      # Formatação consistente (evita drift após o reflow inicial).
      - name: Ruff (format check)
        run: ruff format --check analyzer main.py tests

      # Tipagem não-bloqueante durante a fase de tipagem gradual.
      - name: Mypy (gradual — informativo)
        run: mypy analyzer || true

      # Testes + cobertura bloqueantes (piso ratchet: fail_under = 75 em pyproject).
      - name: Pytest (+ coverage)
        run: pytest --cov=analyzer --cov-report=term-missing --cov-fail-under=75
