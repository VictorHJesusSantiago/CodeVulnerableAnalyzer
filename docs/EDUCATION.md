# Modo education

O modo educacional explica impacto, origem, sink, CWE, validação e remediação.
Ele deve complementar o achado técnico, não substituir revisão de contexto.

Exemplo: em SQL injection, procure entrada não confiável, acompanhe o fluxo até
a consulta e substitua concatenação por parâmetros nativos do driver.
